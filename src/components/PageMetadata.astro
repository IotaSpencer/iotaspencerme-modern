---
interface Props {
	breadcrumb?: string;
	categories: string[];
	tags: string[];
	links: Record<string, string>;
	date?: Date;
	updatedDate?: Date;
}

import FormattedDate from './FormattedDate.astro';

const { breadcrumb, categories, tags, links, date, updatedDate } = Astro.props;
const hasMetadata = Boolean(
	breadcrumb || date || categories.length > 0 || tags.length > 0 || Object.keys(links).length > 0,
);
---

{hasMetadata && (
	<nav aria-label="Page metadata" class="page-metadata">
		{breadcrumb && <span class="breadcrumb">{breadcrumb}</span>}
		{date && (
			<div class="metadata-group published">
				<span class="metadata-label">Published</span>
				<FormattedDate date={date} />
				{updatedDate && (
					<span class="updated-date">
						Updated <FormattedDate date={updatedDate} />
					</span>
				)}
			</div>
		)}
		{categories.length > 0 && (
			<div class="metadata-group">
				<span class="metadata-label">Filed under</span>
				<ul aria-label="Categories">
				{categories.map((category) => <li>{category}</li>)}
				</ul>
			</div>
		)}
		{tags.length > 0 && (
			<div class="metadata-group">
				<span class="metadata-label">Tagged</span>
				<ul aria-label="Tags">
				{tags.map((tag) => <li>{tag}</li>)}
				</ul>
			</div>
		)}
		{Object.entries(links).length > 0 && (
			<div class="metadata-group">
				<span class="metadata-label">Explore</span>
				<ul aria-label="Related links">
				{Object.entries(links).map(([label, href]) => (
					<li><a href={href}>{label}</a></li>
				))}
				</ul>
			</div>
		)}
	</nav>
)}

<style>
	.page-metadata {
		display: flex;
		flex-wrap: wrap;
		gap: 0.35rem 1.25rem;
		align-items: center;
		margin: 2.5rem auto 0;
		padding: 1rem 1.1rem;
		max-width: 720px;
		border-top: 1px solid rgb(var(--gray-light));
		border-bottom: 1px solid rgb(var(--gray-light));
		background: linear-gradient(90deg, rgba(var(--gray-light), 0.18), transparent 60%);
		font-size: 0.78rem;
		line-height: 1.4;
		color: rgb(var(--gray));
	}
	.breadcrumb,
	.metadata-group {
		display: inline-flex;
		align-items: baseline;
		gap: 0.45rem;
	}
	.breadcrumb {
		font-weight: 700;
		color: rgb(var(--gray-dark));
	}
	.metadata-label {
		font-size: 0.68rem;
		font-weight: 700;
		text-transform: uppercase;
		color: var(--accent);
	}
	.page-metadata ul {
		display: flex;
		flex-wrap: wrap;
		gap: 0.2rem 0.65rem;
		margin: 0;
		padding: 0;
		list-style: none;
	}
	.page-metadata li {
		position: relative;
	}
	.page-metadata li:not(:last-child)::after {
		content: '/';
		position: absolute;
		right: -0.45rem;
		color: rgb(var(--gray-light));
	}
	.page-metadata a {
		font-weight: 700;
		text-decoration-thickness: 1px;
		text-underline-offset: 0.18em;
	}
	.published {
		font-weight: 700;
		color: rgb(var(--gray-dark));
	}
	.updated-date {
		font-weight: 400;
		font-style: italic;
		color: rgb(var(--gray));
	}
	@media (max-width: 720px) {
		.page-metadata {
			margin-top: 2rem;
			padding-inline: 0.75rem;
		}
	}
</style>